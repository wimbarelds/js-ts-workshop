## Setup

Nu gaan we typescript toevoegen

## In de terminal

- Run command: <code className="cmd">npm i -D typescript tsx</code>
- Run command: <code className="cmd">npx tsc --init</code>

## Uitproberen

Maak een `myscript.ts` bestand en run met:

```bash
npx tsx myscript.ts
```