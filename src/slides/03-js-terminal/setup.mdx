import { ShortcutButton } from './ShortcutButton';

# Javascript in VSCode (met nodejs)

## Setup

We gaan nu naar VSCode, en gaan nu de javascript in de terminal uitvoeren.

- Open een nieuwe, lege map in VSCode
- Open de terminal in VS Code <ShortcutButton text="ctrl + `" keys={['Control', '`']} />
- Run command: <code className="cmd">npm init</code>
- Enter enter enter...
